import 'package:flutter/material.dart';

class FileImagePreview extends StatelessWidget {
  const FileImagePreview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
